import 'package:flutter/material.dart';

class TaskFlowScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Task Flow")),
      body: Center(child: Text("This is my Task Flow screen")),
    );
  }
}
